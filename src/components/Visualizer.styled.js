import styled from 'styled-components';

export const MainImage = styled.div`
  margin: 0 auto 10px;
  text-align: center;
  cursor: pointer;

  img {
    margin: 0 auto;
    max-width: 100%;
    -ms-flex-negative: 0;
    flex-shrink: 0;
    max-height: 475px;
  }
`;
