import React from 'react';
import * as S from './LoadSpinner.styled';

const LoadSpinner = () => (
  <S.SpinnerContainer className="SpinnerContainer">
    <S.Spinner />
  </S.SpinnerContainer>
);

export default LoadSpinner;
